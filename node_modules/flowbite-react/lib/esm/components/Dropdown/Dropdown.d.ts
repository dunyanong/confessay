import type { FC, PropsWithChildren, ReactNode } from 'react';
import React from 'react';
import type { ButtonProps } from '../Button';
import type { FloatingProps } from '../Floating';
export interface FlowbiteDropdownTheme {
    target: string;
    base: string;
    animation: string;
    hidden: string;
    style: {
        dark: string;
        light: string;
        auto: string;
    };
    header: string;
    content: string;
    arrow: {
        base: string;
        style: {
            dark: string;
            light: string;
            auto: string;
        };
        placement: string;
    };
    item: {
        base: string;
        icon: string;
    };
    divider: string;
}
export interface DropdownProps extends PropsWithChildren<Pick<FloatingProps, 'placement' | 'trigger'>>, ButtonProps {
    label: ReactNode;
    inline?: boolean;
    floatingArrow?: boolean;
    arrowIcon?: boolean;
    dismissOnClick?: boolean;
}
export declare const Dropdown: FC<DropdownProps> & {
    Item: FC<import("./DropdownItem").DropdownItemProps>;
    Header: FC<PropsWithChildren<React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement>>>;
    Divider: FC<{}>;
};
