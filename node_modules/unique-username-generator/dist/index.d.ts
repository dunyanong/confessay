declare type Style = "lowerCase" | "upperCase" | "capital";
export interface Config {
    dictionaries: string[][];
    separator?: string;
    randomDigits?: number;
    length?: number;
    style?: Style;
}
export declare function generateFromEmail(email: string, randomDigits?: number): string;
export declare function generateUsername(separator?: string, randomDigits?: number, length?: number): string;
export declare function uniqueUsernameGenerator(config: Config): string;
export { adjectives, nouns } from "./data";
